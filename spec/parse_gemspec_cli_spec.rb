require 'spec_helper'

describe ParseGemspecCli do
  it 'has a version number' do
    expect(ParseGemspecCli::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
