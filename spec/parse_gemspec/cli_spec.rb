require 'spec_helper'

describe ParseGemspec::Cli do
  it 'has a version number' do
    expect(ParseGemspec::Cli::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
